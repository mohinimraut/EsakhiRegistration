import { PrepopulatefnDirective } from './prepopulatefn.directive';

describe('PrepopulatefnDirective', () => {
  it('should create an instance', () => {
    const directive = new PrepopulatefnDirective();
    expect(directive).toBeTruthy();
  });
});
